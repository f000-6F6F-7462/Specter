# MQTT support requires a server name and JetStream.
server_name: specter

port: 4222
http_port: 8222

jetstream {
  store_dir: /data
  # Bounded so events can never fill the device's disk or memory.
  max_file_store: 1GB
  max_memory_store: 64MB
}

mqtt {
  port: 1883
}
